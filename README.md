# DreamWeaver
DreamWeaver lets users create persistent narrative worlds powered by AI. The user writes a 3-sentence story idea. The app generates a complete world (locations, characters, plot threads), then lets them enter it as an infinite text-based role-playing experience. 
